GEMINI_API_KEY=your_gemini_api_key_here
PORT=3000

# Cosine similarity threshold for retrieval. Below this, we say "not available"
# instead of calling the chat model. See README for how this was tuned.
SIMILARITY_THRESHOLD=0.65
